version: 2.1

jobs:
  trymote:
    docker:
      - image: circleci/python
    steps:
      - run: |
          ls /toto
          echo "Same step as failing command"
      - run:
          command: echo "This ran"
          when: always
    
 
workflows:
  welcome:
    jobs:
      - trymote
