version: 2.1

jobs:
  trymote:
    docker:
      - image: circleci/python

    steps:
      - run:
#           shell: /bin/bash +eo pipefail
          command: |
            ls /toto
            true
      - run:
          command: echo "This ran"
#           when: always

 
workflows:
  welcome:
    jobs:
      - trymote
