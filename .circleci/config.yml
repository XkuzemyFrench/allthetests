version: 2.1

jobs:
  trymote:
    docker:
      - image: circleci/python
    steps:
      - run: |
          ls /toto
      - run:
          command: |
            set +eo pipefail
            echo "This ran"
          when: always
    
 
workflows:
  welcome:
    jobs:
      - trymote
