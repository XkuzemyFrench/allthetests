version: 2.1

jobs:
  trymote:
    docker:
      - image: circleci/python
    steps:
      - run: |
          set +eo pipefail
          ls /toto
      - run:
          command: echo "This ran"
#           when: always
    
 
workflows:
  welcome:
    jobs:
      - trymote
