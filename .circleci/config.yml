version: 2.1

jobs:
  build:
    parameters: 
      condition:
        type: boolean
        
    docker:
      - image: circleci/python
    steps:
      - run: echo "Hello there"
      - run: echo << parameters.condition >>
      
workflows:
  version: 2
  build:
    jobs:
      - build
