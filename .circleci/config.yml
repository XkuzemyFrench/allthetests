version: 2.1

jobs:
  trymote:
    parameters: 
      condition:
        type: string
        
    docker:
      - image: circleci/python
    steps:
      - run: echo "Hello there"
      - run: echo << parameters.condition >>
      
workflows:
  version: 2
  welcome:
    jobs:
      - trymote
